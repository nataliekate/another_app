<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport"
    content="width=device-width, initial-scale=1.0">
  <title>🧊</title>
  <link rel="stylesheet"
    href="/styles.css">
</head>

<body>
  <div class="container">
    <h1>🧊🧊🧊🧊</h1>

    <% if
      (statusMessage)
      {
      %>
      <div
        class="status-message <%= statusMessage.includes('Error') ? 'error' : 'success' %>">
        <%= statusMessage
          %>
      </div>
      <% }
        %>

        <form id="generate_form"
          action="/generate-file"
          method="POST">
          <div class="form-field">
            <label for="name">Your Name</label>
            <input type="text"
              id="name"
              name="name"
              placeholder="Enter your name"
              required
              value="<%= typeof formData !== 'undefined' && formData.name ? formData.name : '' %>">
          </div>

          <div class="form-field">
            <label for="email">Your Email</label>
            <input type="email"
              id="email"
              name="email"
              placeholder="Enter your email"
              required
              value="<%= typeof formData !== 'undefined' && formData.email ? formData.email : '' %>">
          </div>

          <div class="form-field">
            <label for="message">Message (Optional)</label>
            <textarea id="message"
              name="message"
              placeholder="Enter your message"
              rows="4"><%= typeof formData !== 'undefined' && formData.message ? formData.message : '' %></textarea>
          </div>

          <div class="form-field">
            <label for="configFileName">Desired File Name (without
              .json)</label>
            <input type="text"
              id="configFileName"
              name="configFileName"
              placeholder="e.g., my-settings"
              required
              value="<%= typeof formData !== 'undefined' && formData.configFileName ? formData.configFileName : 'my-config' %>">
          </div>

          <div class="btn-group">
            <button class="primary" type="submit">Generate & Download File</button>
            <button>Reset</button>
          </div>
        </form>
  </div>
</body>

</html>